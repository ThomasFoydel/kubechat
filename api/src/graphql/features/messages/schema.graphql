type Message {
  id: ID!
  content: String!
  createdAt: String!
  userId: ID!
  conversationId: ID!
}

input CreateMessageInput {
  content: String!
}

extend type Mutation {
  createMessage(
    conversationId: ID!
    input: CreateMessageInput!
  ): Message!
}
