apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubechat-api
spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kubechat-api
                port:
                  number: 3000